# Maniot-Editor
Maniot is a text editor for the terminal that has multi-tab text editing, directory traversal, and basic file operations. It has undo/redo functions, safe overwrite, and automatic backing up for text edits. It is lightweight with a modular platform and is multiplatform with minimal dependencies.
